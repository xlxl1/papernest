# ── SiliconFlow（硅基流动）推荐配置 ──
# 控制台：https://cloud.siliconflow.cn  → API 密钥
# 填好后先跑：python cli.py models   （列出你账号实际可用的模型 ID，如与下面不一致就改成列表里的）
LLM_API_BASE=https://api.siliconflow.cn/v1
LLM_API_KEY=
LLM_MODEL=qwen3.7-flash
LLM_MODEL_HEAVY=deepseek-v4-pro-0813
# ── Embedding（可以与 chat 用不同的端点，强烈建议分开配）──
# 本项目真实踩过、代价 0.27 Recall 的坑，两个原因叠在一起：
#   1) EMBED_MODEL 填了控制台上的**中文显示名**，而不是 API 的真实模型 ID；
#   2) chat 用的中转端点根本没有 /embeddings 路由（恒 404）。
# 结果是向量检索一直返回空、**悄悄退化成纯 FTS 却不报错**，
# 自建集 Recall@5 只有 0.6771；修好后 0.9427——全部来自修配置，不是算法改进。
# 现在代码会在「配了 EMBED_MODEL 却匹配不到任何向量」时明确上报降级。
#
# 用 `python cli.py models` 列出端点上的真实 ID 再填。
EMBED_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
EMBED_MODEL=qwen3.7-text-embedding
# EMBED_API_KEY 不填则依次回退 DASHSCOPE_API_KEY → LLM_API_KEY
EMBED_API_KEY=

# ── Cross-Encoder 精排（可选）──
# 注意 DashScope 的 rerank **不在 OpenAI 兼容路径下**（那里 404），走原生地址。
# 换 Cohere / Jina / SiliconFlow 时它们都是 <base>/rerank 的 Cohere 风格，改 URL 即可。
PAPERNEST_RERANK=off          # api | llm | bm25 | off（bm25 实测净负收益，别开）
PAPERNEST_RERANK_MODEL=qwen3.7-text-rerank
PAPERNEST_RERANK_URL=https://dashscope.aliyuncs.com/api/v1/services/rerank/text-rerank/text-rerank

# ── 本地模型（Ollama / llama.cpp，可选）──
# 两者都提供 OpenAI 兼容端点，PaperNest 零改动即可接入（隐私敏感场景全离线跑）：
#   LLM_API_BASE=http://localhost:11434/v1
#   LLM_API_KEY=ollama                      # Ollama 不校验，但不能为空
#   LLM_MODEL=qwen3.7:14b
#   EMBED_MODEL=nomic-embed-text
# 注意：换 embedding 模型后向量按 model 列隔离、不会混维度，但旧向量不再被检索到，
# 跑一次 `python cli.py embed` 会先做嵌入空间一致性检测（余弦 ≥0.98 原地迁移，否则重建）。
# 小模型对「只输出 JSON」的遵从较差：解析失败会走各模块的 markdown 兜底并如实标 degraded。

# ── 数据源 ──
# Semantic Scholar 免费申请：https://www.semanticscholar.org/product/api#api-key
# 不填也能用（共享池限流更严，429 时自动退避 / OpenAlex 兜底）
S2_API_KEY=
# OpenAlex 礼貌池（可选）：填你自己的邮箱能进更快的队列；不填走匿名池
OPENALEX_MAILTO=

# ── 课题描述（卡片「与我课题的关系」一栏的上下文）──
RESEARCH_TOPIC=大模型 Agent 的构建与评测

# ── 出网代理（可选）──
# OpenAlex / arXiv 在部分网络下被阻断；开启 Clash 后填 http://127.0.0.1:7897
PAPERNEST_PROXY=

# ── 服务与检索（可选，均有合理默认）──
# 访问口令。**绑定非回环地址时是强制的**：`cli.py serve --host 0.0.0.0` 或走 docker
# 而没设它，服务会**拒绝启动**（而不是悄悄跑起一个谁都能删数据、烧 LLM 额度的接口）。
# 本机自用（默认绑 127.0.0.1）不用设。设了之后网页端会在首次 401 时提示输入，
# 口令存在浏览器 localStorage 里，不随代码分发。
PAPERNEST_API_KEY=
# 注意：口令必须是 **ASCII**（字母数字与常见符号）。HTTP 请求头装不下非 ASCII，
# 填了中文口令服务会拒绝启动并说明原因。
# 明确承担裸奔风险的逃生口（内网隔离环境 / CI）。设成 1 才会跳过上面那道启动自检。
PAPERNEST_ALLOW_NO_AUTH=
# 服务实际绑定的地址。由启动方声明（cli.py serve 与 Dockerfile 各自会写），
# 应用据此判断「有没有对外暴露」。手工跑 uvicorn 时如实填。
PAPERNEST_BIND_HOST=
# 后台任务并发上限（默认 3）
PAPERNEST_MAX_JOBS=3
# L2 全文页级检索：默认开；权重 0.5 是两个评测集一起测出来的（README 有对照表）
PAPERNEST_PAGE_SEARCH=1
PAPERNEST_PAGE_WEIGHT=0.5
# 追问改写口径：heuristic（默认，0 token）| llm | off
PAPERNEST_QUERY_REWRITE=heuristic

# ── 计价表（可选，USD / 1M tokens）：键为模型 ID 或 ID 前缀，值 [prompt, completion] ──
# 配了之后 stats / llm_calls 会落成本；不配则 cost 记空（如实不编造）
# LLM_PRICES_JSON={"gpt-5.6-sol": [2.0, 8.0], "qwen3.7-flash": [0.1, 0.8]}

# ── 每日用量闸门（防失控循环烧光额度，不是限流）──
# prompt + completion 之和，含 embedding。真库观测的每日峰值是 117 万，
# 默认 300 万约为其 2.5 倍：正常的重活（全库重嵌、批量精读）撞不到，
# 失控循环会在几分钟内冲过去。设 0 显式关闭。
PAPERNEST_DAILY_TOKEN_BUDGET=3000000

# ── 日志（本机单用户：一个会轮转的文件就够）──
# 写到 data/papernest.log，5MB × 3 份。调试时设 DEBUG。
PAPERNEST_LOG_LEVEL=INFO

# 视觉模型（插图理解，cli.py figure-summary）。**默认空 = 关掉这个功能**：
# 它按图片像素计费，给一个默认值等于替你决定花钱。
# DashScope 上可用 qwen3-vl-plus（更准）/ qwen3-vl-flash（更便宜）。
# 实测 60 张图约 7 万 token；说明只写图的结构与比较关系，不读数值——
# 让它读数会把折线图的结论说反（见 papernest/figures.py）。
#PAPERNEST_VISION_MODEL=qwen3-vl-plus

# 扫描件 OCR（没有文本层的 PDF）。**默认 off**：云端约 4850 token/页，
# 本地约 17s/页，两样都不该在你没说话时发生。
#   local  RapidOCR，不花钱，需要 pip install rapidocr_onnxruntime
#   cloud  qwen-vl-ocr，更准（合成扫描件实测：词面精确率 95.8% vs 本地 85.9%，
#          召回 92.7% vs 91.3%），但要花钱
# 注：那组数字来自「真论文渲染成图再去文本层」的合成件，没有倾斜/噪点/装订阴影，
#     是**上界**，真扫描件只会更差。对比方法见 tools/ocr_bench.py。
#PAPERNEST_OCR=local
#PAPERNEST_OCR_DPI=200
#PAPERNEST_OCR_CLOUD_MODEL=qwen-vl-ocr-latest

# 表格摘要用哪个模型（留空 = 跟 LLM_MODEL 走）。单独开这个口子是因为
# **它和主模型的可用性可能不同**：2026-09-09 账户欠费后，249 个模型里只剩 8 个
# 能调，主模型不在其中，而 qwen3.5-omni-plus 在。
# 实测同一张表：glm-5.2 平均 1427 token，qwen3.5-omni-plus 只要 547（不出思考 token）。
#PAPERNEST_TABLE_MODEL=qwen3.5-omni-plus
