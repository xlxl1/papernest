# PaperNest Agent 应用开发面试问答

> 核心定位：PaperNest 不是“让多个 Agent 自由讨论并自动写论文”，而是一个**以证据约束为中心、由确定性工作流控制 LLM 不确定性的 Research Agent 系统**。

## 一分钟项目介绍

PaperNest 是我独立设计实现的个人科研工作台，主要解决科研过程中“文献来源分散、检索不准、生成内容难以核验、长流程容易中断”等问题。系统接入学术 API、本地 PDF 和 BibTeX、RIS、Zotero 等来源，将论文处理为元数据、物理页、章节块和表格块；检索侧融合向量检索、标题摘要 FTS 和章节全文 FTS，通过 RRF 进行排序，并使用受控的伪相关反馈补充候选。

Agent 侧没有采用开放式自主循环，而是通过确定性 Router、工具注册表和有界执行器，组合调用检索、精读、问答、引用推荐和综述生成等工具。生成内容受到引用白名单和原文证据回取约束；长任务通过持久化任务表、SSE、并发限制和分阶段检查点执行。整体设计原则是：**让 LLM 负责语义理解与生成，让确定性程序负责流程边界、证据约束和故障处理。**

## 一、项目定位与 Agent 设计

### 1. 这为什么叫 Agent，不就是 RAG 吗？

RAG 是系统中的一个工具能力。Agent 层还负责识别任务、选择工具、传递中间状态、处理步骤依赖、执行失败和记录轨迹。普通 RAG 通常是固定的“检索一次然后生成”，PaperNest 可以根据目标分别进入检索问答、论文精读、引用推荐或综述生成等路径。

### 2. “自研 Research Agent”具体自研了什么？

自研的是 Agent 编排层，包括确定性路由、计划结构、工具注册、有界执行、状态传递、超时重试和轨迹持久化。底层模型使用 OpenAI 兼容接口，并没有训练基础模型，因此更准确的表述是“自主实现工具型 Agent 编排层”，而不是“自研大模型”。

### 3. 为什么不用 LangChain、LangGraph 或 CrewAI？

当前任务空间有限，工具数量固定，核心诉求是可控、可测试和可复现。直接实现编排层更容易看清状态、异常与数据流，也减少框架依赖。如果流程以后发展为动态 DAG、并行分支和复杂补偿事务，LangGraph 一类框架会更有价值。

### 4. 为什么 Planner 是确定性的？

工具集合和主要用户意图是已知的，没有必要让 LLM 决定任意工具名和参数。确定性 Planner 可以避免非法工具调用、参数幻觉、循环失控和额外模型成本，也能让相同输入得到稳定计划。

### 5. 关键词 Router 会不会太简单？

会，它是当前版本主动选择的可解释基线，不是通用意图识别器。优点是行为稳定、容易测试；缺点是复合意图和隐式表达识别能力有限。下一步可以让 LLM 只生成受 schema 约束的候选计划，再由程序校验工具名、参数、步数和权限。

### 6. 什么叫“有界状态机”？

请求状态受限于 `planned/completed/blocked/failed`，工具事件受限于 `running/completed/failed/timeout`，同时有 `max_steps` 和单步超时。当前实现不是一个通用状态机引擎，本质上是具有显式状态约束的顺序执行器；面试中这样描述更准确。

### 7. Research Agent 有哪些工具？

当前核心注册表包含五个工具：本地文献检索、RAG 问答、引用推荐、综述生成和论文精读。工具统一返回字典，API 请求、计划和事件使用 Pydantic 模型约束；内部状态仍是普通字典，因此不能声称“全链路强类型”。

### 8. 为什么把检索和回答拆成两个工具？

检索结果会显式进入 Agent 状态，问答工具直接消费上一步的候选论文 ID，不会隐藏地再检索一次。这样执行轨迹可以准确说明回答使用了哪次召回结果，也便于单独替换或评测某个环节。

### 9. 如何避免 Agent 无限循环？

当前 Planner 只生成有限计划，执行器最多运行 `max_steps` 个步骤，没有让模型自行决定是否继续调用工具。迭代问答同样限制最大轮数，并将上下文文献数限制为最多 12 篇。

## 二、多 Agent 写作

### 10. 五个 Agent 真的是五个独立智能体吗？

不是五个自由对话的独立进程，而是五个职责隔离的角色节点：文献、选题、大纲、撰写和润色。它们使用不同的 prompt、输入输出和持久化产物，由固定 DAG 编排。准确说法是“角色化多 Agent 工作流”。

### 11. 为什么不直接让一个 Agent 写到底？

长文一次生成很难控制结构、引用和局部修改。拆分后，每个阶段都有明确产物，例如选题候选、大纲 JSON、章节草稿、引用白名单和终检报告；某一节失败或被修改时，只需重跑该节，不必重新生成全文。

### 12. Agent 之间如何传递上下文？

主要通过数据库中的结构化产物传递，而不是复制整段自然语言对话。例如大纲包含章节、论点、字数和候选论文 ID；章节阶段保存白名单、正文、引用映射和评审结果。这可以降低上下文膨胀和语义漂移。

### 13. 人工检查点设置在哪里？

选题完成后由用户确认题目，大纲完成后由用户确认或修改结构；每次确认后才提交下一阶段任务。检查点同时也是持久化边界，因此中断后可以从已完成阶段重新提交后续任务。

### 14. 如何控制章节质量？

每节生成后进入评审闭环。评分低于 7 分或存在 `major` 问题时，系统携带评审意见进行重写，最多重写配置允许的次数。达到上限仍不合格时会保留结果并显式记录分数，而不是假装已经通过。

### 15. 多 Agent 是否带来了量化提升？

当前主要验证了流程可控性、局部重跑和引用约束，还没有完成“单 Agent 对比五角色 Agent”的完整质量实验。因此不能直接声称多 Agent 将质量提升了某个百分比，这应当作为后续消融实验。

## 三、检索与 RAG

### 16. 混合检索具体如何实现？

系统有三路召回：论文及章节向量检索、标题摘要 FTS、章节全文 FTS。它们通过加权 RRF 融合：

```text
score(d) = sum(w_i / (60 + rank_i + 1))
```

标题摘要和向量检索的权重为 1，章节全文当前权重为 0.2，避免正文中的偶然词频接管排序。

### 17. 为什么选择 RRF？

向量相似度和 FTS 分数不在同一量纲，直接相加需要不稳定的分数归一化。RRF 只依赖各路排名，对召回器的原始分数分布不敏感，同时允许某一路不可用时自然退化。

### 18. PRF 是怎么实现的？

首轮召回后，系统从头部论文的标题短语和关键词中确定性抽取扩展查询，不调用 LLM。扩展查询得到的新论文默认追加在原始结果之后，不改变原始结果的相对顺序，主要用于扩大长上下文场景中的候选覆盖。

### 19. 为什么不把扩展查询直接参与 RRF？

消融实验发现，扩展词容易发生 query drift。将它们参与主排序，会让宽泛候选挤掉原始查询召回的正确结果。因此系统保留 RRF 融合作为负实验，线上默认采用“原始排序不变、扩展候选补位”的策略。

### 20. PDF 为什么同时保存物理页和章节块？

两者解决的问题不同：物理页用于证据页码回查，章节块用于语义完整的检索。表格会单独成块，过长表格按整行切分并在后续块中重复表头；章节识别失败时则退化为按页切分。

### 21. 如何处理中文提问和英文论文之间的表达差异？

向量召回承担主要的跨语言匹配。页面选择如果无法用中文查询命中英文正文，会退回使用论文标题和关键词选页，并把这次降级写入结果。当前没有接入专门的跨语言重排模型，这是可以继续优化的方向。

### 22. 为什么没有强行加入 reranker？

部分重排方案的消融结果显示，当候选集已经接近饱和时，重排只能调整已有候选顺序，不能找回未被召回的论文；纯词面重排还可能损失向量召回带来的贡献。因此没有为了技术栈完整而增加一个没有稳定收益的线上层级。

## 四、证据约束与幻觉控制

### 23. “证据约束”具体指什么？

引用候选不仅包含论文 ID，还要包含可以从原文中回取的证据句；写作 Agent 只能使用白名单内的引用编号，越界编号会被剥离并记录；全文关键结论保存页码并回到对应页面检查；最终再检查悬空引用、未核验引用和“待补证据”。

### 24. 机械回取能够证明结论正确吗？

不能。机械回取主要证明“证据句确实存在于这篇论文”，解决的是来源伪造问题；向量相似度只能近似判断相关性，不能严格证明语义蕴含。更严格的方案需要 NLI、交叉编码器或人工核验。

### 25. 所有引用都可以定位到页码吗？

目前不能这样声称。全文精读的关键结论和文档来源能够定位到物理页；引用推荐主要从摘要句中回取，通常只有论文级来源。简历应写“全文证据支持页码回查”，不要写成“所有引用均可按页回查”。

### 26. 证据不足时系统怎么处理？

问答会提示“库内文献未覆盖”；迭代问答会单独列出仍无引用的论断；写作阶段使用“待补证据”；综述中未通过检查的句子会保留并附加未通过标记。原则是显式暴露不确定性，而不是静默删除或伪造依据。

### 27. 引用白名单如何限制模型？

文献 Agent 先为每节构造通过证据回取的候选列表，撰写 Agent 只能使用该列表的局部编号。生成完成后，程序用正则删除白名单范围外的编号并记录删除项；装配全文时再将各节局部编号映射为全局编号。

### 28. “证据核验率”和“引用准确率”有什么区别？

证据核验率表示返回的证据句是否确实存在于来源文本中；引用准确率表示推荐论文是否命中人工标注的 gold 文献。前者高只能说明来源真实，不能说明论文一定推荐正确，两者不能混用。

## 五、对话记忆与上下文控制

### 29. 多轮对话记忆如何实现？

会话、消息、每轮实际使用的来源和来源编号都会持久化。追问只在检索阶段补全查询，历史回答只能用于理解指代，不能被当成新的事实来源；同一篇论文在同一会话中的 `[n]` 编号保持稳定。

### 30. 如何识别“它有什么局限”这类追问？

系统通过中文指代词、英文词边界和“第 n 篇”一类显式编号识别追问。默认把最近的用户问题与当前追问拼接成独立检索查询，也可以切换为 LLM 改写；改写只影响检索，不直接修改用户最终问题。

### 31. 如何防止上下文无限增长？

普通问答设置约 24000 字符的上下文预算，迭代问答最多使用 12 篇论文。篇数较多时会降低每篇进入上下文的页块数量；超出预算的论文不会进入来源列表，避免出现“来源列表有编号但模型没有看到对应上下文”的错位。

## 六、后台任务与工程可靠性

### 32. 后台任务系统提供什么保证？

SQLite 保存任务状态和当前进度，有界线程池默认最多并发三个任务。写作阶段通过 `dedupe_key` 拦截同一 run 的重复提交，并通过原子的 `queued -> running` 状态更新避免同一个任务被重复认领。

### 33. 服务重启后任务会自动续跑吗？

不会。启动时会把遗留的 `queued/running` 任务标记为失败，避免客户端无限等待；写作流程可以从已持久化的阶段重新提交。准确说法是“中断任务识别与阶段级续作”，不是“自动恢复任务”。

### 34. 超时是否真正终止了工具？

当前超时会停止等待并立即返回失败，但 Python 后台线程可能仍然运行，无法做到强制终止。这是明确的实现限制。对于具有强副作用的长任务，后续应改成独立进程、协作取消或带租约的外部任务队列。

### 35. 重试策略是什么？

Agent 工具层的普通异常最多重试一次，配置缺失不重试，步骤超时也不重试。LLM HTTP 层对网络异常、429 和 5xx 最多尝试四次，并使用指数退避加随机抖动；400、401、403 等确定性错误立即失败。流式输出已经产生内容后不重试，避免生成重复文本。

### 36. 为什么使用 SSE 而不是 WebSocket？

任务进度是服务端到客户端的单向流，SSE 实现更简单，浏览器原生支持事件流，并可使用心跳防止代理断开。需要区分两种能力：任务 SSE 推送和恢复的是最新状态快照；Agent run 保存的是完整工具事件时间线。

### 37. 任务系统能保证 exactly-once 吗？

不能声称分布式 exactly-once。当前系统通过重复任务拦截、原子认领和章节表唯一键实现单机范围内的幂等保护，但它不是分布式队列，也没有租约、ack 和跨节点协调机制。

### 38. 为什么使用 SQLite？

这是面向个人科研工作台的单机系统，SQLite 部署简单、事务能力足够，并同时承载元数据、FTS、任务状态和执行轨迹。系统通过 WAL、busy timeout 和短连接控制并发；如果扩展到多用户、多实例部署，再迁移 PostgreSQL 和独立任务队列更合理。

### 39. 章节缓存是怎么设计的？

当前缓存 key 包含 prompt 版本、章节标题、论点、字数和术语表，未变化的章节可以零 LLM 调用复用。一个已发现的问题是，key 尚未包含引用白名单、模型和生成参数，文献变化后可能复用旧引用，这是应优先修正的缓存失效风险。

### 40. Milvus 是否承载了线上检索？

是。`PAPERNEST_VECTOR_BACKEND` 默认为 `milvus`，线上向量检索由 Milvus 承载；`MilvusStore` 是 `vectorstore` 抽象层的第三个可插拔实现，与 numpy、Chroma 并列。SQLite 的 `vectors` 表是唯一真相来源，Milvus 只是派生索引：写路径先落 SQLite 再同步 Milvus，读路径的正文也从 SQLite 回取，Milvus 中查到但 SQLite 已删除的行会被丢弃并计入 `last_stale_hits`，`rebuild()` 随时可以从 SQLite 全量重放。连不上 Milvus 时抛出 `VectorStoreUnavailable` 而不静默降级，只有显式设置 `PAPERNEST_VECTOR_FALLBACK=1` 才回退到 numpy 并在 `store.degraded` 留痕。没有外部向量服务的单容器形态可以用 `PAPERNEST_VECTOR_BACKEND=numpy` 一行切回。实测当前规模下 numpy 精确检索更快（3.3ms 对 20.1ms），选 Milvus 是部署形态上的决定，HNSW 的 top-5 与精确检索 30/30 一致，没有召回损失。

## 七、评测与实验设计

### 41. Recall@15 的评测口径是什么？

评测集共 50 条，其中 **32 条是检索题、8 条是引用题、10 条是 QA 题**。Recall 对每条查询计算：

```text
|top-K 命中的 gold 文献| / |该查询的全部 gold 文献|
```

最后对查询做宏平均。gold 使用 DOI 或 arXiv ID 生成的归一化键，而不是数据库自增 ID。

### 42. `Recall@15 0.766 -> 0.928` 可以写进简历吗？

**当前不可以。仓库中没有实验记录支持 0.928。**

- README 历史固定快照记录：`0.7656 -> 0.8281`。
- 当前 503 篇文献库复跑：纯 FTS 为 `0.7656`，PRF 为 `0.7812`。
- 关闭章节全文检索后，当前 PRF 结果为 `0.7969`。

面试前应冻结数据库快照、评测集、环境变量和代码版本，再使用能够一键复现的数字。否则建议暂时只写“通过消融实验验证 PRF 补位对长上下文召回的改善”。

### 43. 自建评测集会不会有数据泄漏？

有这个风险，尤其当查询是阅读目标论文后编写的。当前结果适合比较不同检索方案，不足以独立证明真实用户场景的泛化能力。应补充盲标查询、时间切分、独立人员出题和公开 QASPER 评测。

### 44. 为什么报告 Recall 而不是只报告准确率？

RAG 首先需要尽可能把可支撑答案的文献送入上下文，因此召回是第一层瓶颈。只看 Precision 可能偏好非常保守的结果；但 Recall 也不够，仍需同时观察引用 precision、证据覆盖、无证据率和最终回答质量。

### 45. 为什么线上数字和离线数字不一致？

两者可能使用不同的向量配置、全文块开关、库规模和外部 embedding 服务状态。向量接口瞬时失败时检索会退化为 FTS，也会改变结果。因此汇报任何指标都必须同时给出 `K`、库快照、题目数量、检索口径、模型和配置。

### 46. 当前评测还有哪些不足？

自建题目规模较小；QA 指标依赖真实模型，稳定性和成本更高；证据核验主要验证来源存在性，没有完全验证语义蕴含；缺少单 Agent 与多 Agent 的质量、成本和时延对比；外部 API 的真实端到端测试也不足。

## 八、测试、局限与演进

### 47. 自动化测试做到了什么程度？

当前环境运行 `python -m unittest discover -s tests -p "test_*.py"`，**1015 项测试全部通过，耗时约 61.5 秒**。测试涉及 Agent、任务系统、检索、PDF 结构、写作流水线、引用约束和向量后端等模块。

不能声称“代码覆盖率达到 xx%”，因为当前没有对应 coverage 报告；也不能把使用 mock 的离线测试说成真实外部模型的端到端测试。

### 48. 项目当前最大的不足是什么？

主要不足包括：Router 对复合意图支持有限；证据验证还不等于严格语义蕴含；任务线程无法真正取消；PRF 效果对语料快照敏感；多 Agent 缺少相对单 Agent 的消融；章节缓存 key 没有包含全部证据和模型版本。

### 49. 如果继续迭代，优先做什么？

1. 冻结评测资产，建立可以一键复现的检索基线。
2. 引入 claim-evidence entailment 模型和人工抽检。
3. 将长任务升级为支持取消、租约和幂等恢复的进程级队列。
4. 为 Milvus 后端补健康探针和多副本共享同一实例的配置。
5. 对 Router、多 Agent、PRF 和缓存分别做质量、成本、时延消融。

### 50. 这个项目最能体现你的什么能力？

不是简单调用模型 API，而是把一个不确定的生成模型放入可测试、可观测、有证据边界的工程系统中。项目体现了检索实验设计、Agent 编排、数据建模、故障处理和对技术局限的判断能力。

## 九、压力面试速答

### “这不就是套壳吗？”

如果只调用一次模型确实是套壳。这个项目的工作量主要在模型外部：多源文献入库、PDF 结构解析、三路混合检索、证据回取、受控工具执行、任务持久化、指标评测和失败降级。模型是其中一个可替换组件，而不是全部系统。

### “多 Agent 是不是为了追热点？”

如果只是换五套 system prompt，确实没有意义。这里拆分的原因是不同阶段有独立输入输出、权限和失败边界，并支持人工检查点、版本化大纲、单节重跑和缓存复用。它更接近角色化工作流，而不是几个模型互相聊天。

### “你的证据核验是不是自欺欺人？”

它只能保证来源可回取，不能保证完整的语义蕴含。我把这两层能力明确区分，没有把字符串回取包装成事实正确性。下一阶段会用 NLI、交叉编码器和人工抽检补充语义验证。

### “为什么你的指标换一次数据就变了？”

因为检索指标依赖语料快照、全文覆盖、向量覆盖和配置。这个问题说明评测资产还没有完全冻结，也是当前工程上最需要补齐的部分。正式汇报前必须把数据库快照、配置和命令一起版本化。

### “如果模型输出完全不遵守格式怎么办？”

结构化阶段先尝试解析 JSON，失败后进入确定性离线模板或显式降级；引用编号还会经过程序清洗和终检。当前没有把模型输出直接当作可信控制指令执行。

### “项目上线后最担心什么？”

最担心的不是模型文风，而是错误证据被包装成可信引用、长任务副作用在超时后继续执行，以及评测口径漂移导致错误判断。因此优先级应放在语义证据验证、可取消任务和版本化评测上。

## 十、简历与面试表述红线

以下说法目前不要使用：

- “自研大模型”——实际自研的是 Agent 编排和业务系统。
- “五个自主 Agent 协商写作”——实际是固定 DAG 中的角色节点。
- “所有引用均支持页码回查”——摘要引用通常只有论文级来源。
- “机械核验证明结论正确”——只能证明证据可以从来源中回取。
- “任务超时后已彻底终止”——后台线程可能仍在运行。
- “服务重启后任务自动续跑”——当前是识别中断并允许阶段级重提。
- “Recall@15 从 0.766 提升到 0.928”——当前没有可复现实验证据。
- “测试覆盖率达到 xx%”——当前只有测试数量，没有 coverage 数据。

## 十一、代码核查入口

- Agent 路由与有界执行：[papernest/agent.py](papernest/agent.py)
- Agent 工具注册表：[papernest/tools.py](papernest/tools.py)
- 混合检索与 RRF：[papernest/embeddings.py](papernest/embeddings.py)
- PRF 与迭代问答：[papernest/deepsearch.py](papernest/deepsearch.py)
- 多 Agent 写作流水线：[papernest/pipeline.py](papernest/pipeline.py)
- 持久化任务系统：[papernest/jobs.py](papernest/jobs.py)
- SSE 与 API：[papernest/api.py](papernest/api.py)
- 对话记忆：[papernest/chat.py](papernest/chat.py)
- PDF 全文与页码核验：[papernest/fulltext.py](papernest/fulltext.py)
- 评测指标：[papernest/eval.py](papernest/eval.py)
- 评测集：[eval_set.json](eval_set.json)

## 十二、最终叙事

> 我没有把重点放在“让多个 Agent 自主写论文”，而是围绕科研写作中最难控制的证据可靠性和长流程稳定性，把 LLM 放进一个受约束的工具调用系统。LLM 负责理解和生成，确定性工作流负责路由、状态、引用边界、失败处理和审计；系统追求的不是完全自治，而是可控、可回查和可继续执行。
